[project]
name = "aeryn-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "ruff>=0.11.8",
    "sqlmodel>=0.0.24",
]
